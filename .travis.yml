---
language: python
python: "2.7"

install: yum install ansible
install: yum install ansible-lint
install: yum install yamllint

script: ansible-playbook ansible_lint ./
script: ansible-playbook yamllint ./
